export const enum UserRole {
    STUDENT = 'student',
    LANDLORD = 'landlord',
    EMPLOYER = 'employer',
    ADMIN = 'admin',
    SUPERADMIN = 'superadmin'
}

export const enum AuthProvider {
    LOCAL = 'local',
    GOOGLE = 'google',
    FACEBOOK = 'facebook',
    WHATSAPP = 'whatsapp'
}
