export enum UserRole {
    GUEST = 'guest',
    STUDENT = 'student',
    LANDLORD = 'landlord',
    EMPLOYER = 'employer',
    ADMIN = 'admin',
    SUPERADMIN = 'superadmin',
}

export enum AuthProvider {
    LOCAL = 'local',
    GOOGLE = 'google',
    FACEBOOK = 'facebook',
    WHATSAPP = 'whatsapp',
}
